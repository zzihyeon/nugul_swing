[project]
name = "stock-swing-agents"
version = "0.1.0"
description = "Korean stock swing-trading assistant agent voting engine."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
swing-agents = "stock_swing_agents.cli:main"

[tool.setuptools]
packages = ["stock_swing_agents"]

